============================================================
TACSFON (OAUSTECH) — FULL-STACK WEBSITE EXECUTION SPECIFICATION
============================================================

PROJECT NAME
TACSFON (OAUSTECH)

OFFICIAL ORGANIZATION
TACSFON — The Apostolic Church Student Fellowship of Nigeria

CURRENT FELLOWSHIP IDENTITY
TACSFON (OAUSTECH)

CURRENT YEARLY THEME
"The Davidic Generation"

IMPORTANT:
The fellowship name, yearly theme, social links, contact information,
donation details, and other organizational information must be easy
to change without requiring changes throughout the UI components.

============================================================
1. ROLE
============================================================

You are a senior full-stack Next.js engineer and UI/UX designer.

Build a polished, production-ready website for TACSFON (OAUSTECH).

Do not build a generic church template.

The website should feel:

- modern
- welcoming
- youthful
- spiritual
- editorial
- clean
- natural
- elegant
- subtle
- trustworthy
- appropriate for a university Christian fellowship

The design should combine:

MODERN CHURCH
+
EDITORIAL DESIGN
+
NATURAL / ORGANIC VISUAL LANGUAGE

Avoid making the website look like:

- a generic SaaS dashboard
- a generic corporate website
- an old-fashioned church website
- an overly luxurious "premium dark" website
- an overly animated landing page

============================================================
2. PRIMARY OBJECTIVES
============================================================

The website should:

1. Introduce TACSFON (OAUSTECH).
2. Explain what the fellowship is about.
3. Communicate the current yearly theme.
4. Display fellowship activities and programs.
5. Display events and programs, including their flyers/images.
6. Explain the different fellowship units.
7. Provide useful information about TACSFON.
8. Allow visitors to contact the fellowship.
9. Allow visitors to access external social/community links.
10. Provide a donation interface.
11. Allow authorized administrators to manage dynamic events.
12. Provide a secure admin dashboard.
13. Be highly responsive.
14. Be accessible.
15. Be SEO-friendly.
16. Be friendly to search engines and AI/agentic browsing systems.

============================================================
3. NON-NEGOTIABLE TECHNOLOGY
============================================================

Use:

- Next.js
- App Router
- TypeScript
- Tailwind CSS
- shadcn/ui
- Zod
- React Hook Form
- Lucide React
- Framer Motion
- Supabase
- Brevo
- Express where backend separation is actually required/appropriate

Do not introduce unnecessary frameworks.

Use server-side functionality provided by Next.js wherever possible.

Do not create unnecessary backend infrastructure when a Next.js
Route Handler or Server Action is sufficient.

============================================================
4. DATABASE DECISION
============================================================

Supabase WILL be used.

Do not attempt to eliminate the database because the admin dashboard
requires persistent dynamic content.

Use Supabase for:

- administrator authentication
- dynamic event storage
- event images/files where appropriate
- required persistent dynamic data

Do NOT turn the project into a complicated CMS.

Keep the database intentionally small.

Static organizational content should remain outside the database.

============================================================
5. AUTHENTICATION
============================================================

Administrators use Supabase Authentication.

IMPORTANT:

There must NOT be a public administrator signup page.

Administrator accounts are created directly through Supabase.

The application should provide:

/admin/login

and authenticated administrator access to:

/admin

Logout must be supported.

Protect all admin routes.

An unauthenticated user must never be able to access admin functionality.

Do not expose Supabase service-role credentials to the browser.

Use appropriate Supabase security practices.

============================================================
6. ADMIN DASHBOARD
============================================================

Build a simple and clean administrator dashboard.

Do NOT over-engineer it.

Suggested structure:

/admin
/admin/login
/admin/events
/admin/events/new
/admin/events/[id]/edit

Dashboard should provide:

- overview
- event management
- account/logout functionality

The dashboard does not need:

- complicated analytics
- complicated CMS functionality
- unnecessary charts
- complex permissions systems
- unnecessary settings pages

============================================================
7. EVENT MANAGEMENT
============================================================

Events are dynamic.

Authenticated administrators can:

- create events
- edit events
- delete events
- publish/unpublish events

Event fields should include at minimum:

- title
- description
- date
- time
- location
- event image/flyer
- optional external link
- published status

Potential additional useful field:

- event category/type

Do not force every event to have every field.

Some events may be simple.

Some may be major programs.

The UI must accommodate both.

============================================================
8. EVENT IMAGE / FLYER SYSTEM
============================================================

Event images/flyers must be supported.

The event editor should make uploading an image simple.

Use Supabase Storage where appropriate.

Validate uploaded files.

Do not allow arbitrary unsafe file types.

The public event interface should display event flyers/images elegantly.

Images must not destroy the visual hierarchy.

Use Next.js Image wherever applicable.

============================================================
9. STATIC VS DYNAMIC CONTENT
============================================================

STATIC CONTENT:

- About TACSFON
- general fellowship information
- units
- fellowship identity
- general beliefs/information
- Bible quotes
- general descriptions
- footer information
- donation information
- social media links
- other organizational information

DYNAMIC CONTENT:

- events
- programs that administrators need to regularly manage
- event flyers
- event dates
- event descriptions
- event links
- published/unpublished status

Do not put every piece of content into Supabase.

============================================================
10. CONTENT FILE
============================================================

A separate content file will be provided by the project owner.

The file contains content direction and organizational information.

Do NOT expect the content file to contain every sentence required for
the website.

Use the provided content as the source of truth.

Where the content gives direction rather than exact copy, write
appropriate polished copy that stays faithful to TACSFON.

Do not invent organizational facts.

If information is genuinely unavailable, use a clearly identifiable
placeholder rather than presenting invented facts as real.

Make changeable content centralized.

Avoid hardcoding organizational information across dozens of components.

============================================================
11. IMAGE ASSET SYSTEM
============================================================

A folder containing website images/assets will be provided.

FIRST ACTION:

Inspect the provided asset folder before designing the website.

Understand:

- available images
- logos
- photographs
- flyers
- icons
- possible videos
- other media

Do NOT use external stock photography.

Do NOT use Unsplash, Pexels, random image URLs, or other stock-image
services.

Use the provided assets.

Create a centralized:

images.ts

file/module.

The purpose of images.ts is to provide a single organized place where
website image references are managed.

Do not scatter image paths throughout components.

Example conceptual structure:

images.ts

- logo
- hero images
- fellowship images
- unit images
- gallery images
- decorative images
- other assets

If optional local videos exist, manage their references in an equally
organized way.

Use meaningful names.

Do not use filenames such as:

image1
image2
final-final
newimage

unless those are unavoidable actual source filenames.

============================================================
12. NO EXTERNAL STOCK IMAGES
============================================================

This is a strict requirement.

Never introduce external stock imagery merely to make the design
look more complete.

If an appropriate local image does not exist:

- use a designed visual treatment
- use typography
- use a gradient
- use an existing appropriate asset
- or use a restrained placeholder

Do not secretly introduce stock images.

============================================================
13. VISUAL DESIGN SYSTEM
============================================================

PRIMARY COLOR:

FOREST GREEN

The exact shade should feel rich and natural rather than neon.

SECONDARY:

WHITE

White should provide generous breathing room.

ACCENT:

MUTED / SOPHISTICATED GOLD

Gold should NOT look like bright yellow.

Use gold primarily for:

- small accents
- highlights
- icons
- borders
- selected states
- decorative details
- occasional CTA emphasis

Do not flood the interface with gold.

OPTIONAL SUPPORTING NEUTRALS:

Use tasteful warm/cool neutral shades as required for hierarchy.

The overall interface should remain predominantly:

FOREST GREEN + WHITE

with restrained GOLD.

============================================================
14. DESIGN CHARACTER
============================================================

The website should have:

- generous whitespace
- strong typography
- editorial hierarchy
- clean grids
- beautiful imagery
- subtle gradients
- subtle borders
- restrained shadows
- moderate corner rounding
- natural spacing
- visual rhythm

Rounded corners:

YES.

But do not use extremely rounded "pill everything" design.

Avoid:

- excessive rounded cards
- excessive shadows
- excessive glassmorphism
- excessive gradients
- excessive borders
- excessive decorative effects

============================================================
15. DARK SECTIONS
============================================================

Dark sections are allowed.

However, do NOT create aggressive dark premium-looking sections.

Deep forest green can be used naturally for:

- CTA sections
- footer
- selected hero areas
- emphasis sections

Do not make the entire website dark.

The site should feel bright, welcoming and breathable.

============================================================
16. GRADIENTS
============================================================

Subtle gradients are encouraged.

Use them to add depth.

Do NOT use:

- loud gradients
- rainbow gradients
- neon gradients
- excessive gradient text

Gradients should feel natural and almost understated.

============================================================
17. BENTO GRID REQUIREMENT
============================================================

BENTO GRID LAYOUTS ARE MANDATORY.

Use Bento layouts throughout the website where they improve the
presentation.

Do NOT force Bento layouts where they make the content worse.

Use CSS Grid + Tailwind for custom Bento compositions.

Bento layouts can be used for:

- fellowship introduction
- units
- activities
- events
- impact/highlights
- resources
- gallery previews
- calls to action
- other appropriate content

Cards should have varied visual hierarchy.

Avoid making every card identical.

A Bento grid should feel intentionally designed rather than being a
collection of random rectangles.

============================================================
18. HOMEPAGE
============================================================

The homepage must remain relatively short.

Do NOT create a giant homepage containing every piece of information.

The homepage should communicate the most important things quickly.

Suggested structure:

1. Navbar
2. Hero
3. Short introduction / identity section
4. Bento section showing key fellowship areas
5. Upcoming / highlighted events
6. Units preview
7. Bible quote / scripture section
8. Small CTA
9. Footer with compact contact interface

Keep sections concise.

The homepage should make users want to explore the rest of the site.

Do not duplicate entire pages inside the homepage.

============================================================
19. HERO
============================================================

Hero should communicate:

- TACSFON (OAUSTECH)
- current theme: The Davidic Generation
- concise statement about the fellowship
- clear CTA(s)

Use supplied imagery where appropriate.

The hero should be visually strong but NOT oversized to the point where
the user sees nothing else.

Avoid generic:

"Welcome to our church"

copy.

Make it specific to a student fellowship.

============================================================
20. SUGGESTED PUBLIC PAGES
============================================================

Create:

/

 /about

 /events

 /units

 /gallery

 /resources or /sermons

 /contact

Do not create a separate Ministries page.

Use:

/units

instead.

Donation should NOT require a dedicated page.

It should be accessible through prominent but tasteful Donate buttons.

Additional pages may be introduced only if they genuinely improve the
website.

============================================================
21. ABOUT PAGE
============================================================

Explain:

- what TACSFON is
- the fellowship's purpose
- its student/community orientation
- values
- current theme where appropriate

Use a visual storytelling approach.

Do not write a wall of text.

Use Bento or editorial layouts where appropriate.

============================================================
22. UNITS PAGE
============================================================

Units are organizations/groups within the fellowship.

Examples may include:

- Bible Study Unit
- Prayer Unit
- Evangelism Unit
- Choir Unit

Do not assume these are the complete list unless confirmed by the
provided content.

The page should focus on:

- what each unit does
- its purpose
- its activities
- how it contributes to the fellowship

Do NOT build this as a staff/member directory.

Do not require individual member information.

Use attractive Bento cards or layouts.

============================================================
23. EVENTS PAGE
============================================================

Display published events.

Each event should communicate:

- title
- date
- time when available
- location when available
- description
- flyer/image when available
- external link when available

Provide an attractive event detail experience.

The event system must distinguish between:

- upcoming events
- past events

where useful.

Do not show unpublished events publicly.

============================================================
24. SOCIAL / EXTERNAL LINKS
============================================================

Support external community/social links.

Platforms currently expected:

- Facebook
- TikTok
- Instagram
- X

DO NOT include YouTube unless later requested.

Actual URLs will be provided separately.

Do not invent social URLs.

Use Lucide icons where available.

============================================================
25. GALLERY
============================================================

Use the supplied local image assets.

Create a visually interesting gallery.

Bento layouts are encouraged.

Do not create an unnecessarily complicated gallery system.

The gallery should feel like a visual story of the fellowship.

============================================================
26. BIBLE QUOTES / SCRIPTURE
============================================================

Include Bible quotes throughout the site where appropriate.

They should be:

- visually elegant
- short
- relevant
- not excessive

Do not turn every section into a Bible verse.

If exact scripture wording is used, preserve accuracy.

============================================================
27. DONATION EXPERIENCE
============================================================

There should be a Donate CTA/button.

There does NOT need to be a dedicated donation page.

Clicking Donate should open a tasteful modal using shadcn/ui.

The modal should display the available:

- bank information
- payment information
- account details
- purpose/instructions

Donation details should be centralized and easy to change.

Users must be able to COPY individual details easily.

Provide appropriate copy feedback such as:

"Copied"

or a suitable accessible equivalent.

Where a donor needs to provide a name/reference/subject, clearly explain
what they should use.

Example:

Name:
[donor name]

Subject / narration:
[what the donation is for]

Do not invent actual bank details.

These will be provided by the project owner.

============================================================
28. CONTACT EXPERIENCE
============================================================

Contact should NOT be a giant standalone form dominating the website.

The primary contact experience should live in/near the footer.

It should collect enough information to make the message useful.

Suggested fields:

- Name
- Email
- Phone (optional)
- Subject / reason for contacting
- Message

Use:

React Hook Form
+
Zod

Client-side validation is required.

Server-side validation is ALSO required.

Never trust frontend validation alone.

============================================================
29. BREVO EMAIL SYSTEM
============================================================

Brevo is used for email delivery.

Flow:

Visitor
   ↓
Contact form
   ↓
React Hook Form
   ↓
Zod validation
   ↓
Next.js server-side endpoint
   ↓
Rate limiting / abuse protection
   ↓
Brevo API
   ↓
Admin receives email
   ↓
Visitor receives confirmation email

The project owner will provide:

- Brevo API key
- sender email
- sender name
- receiving/admin email
- any other required credentials

============================================================
30. BREVO SECURITY
============================================================

BREVO API credentials MUST NEVER be exposed to the client.

Do not put Brevo credentials into:

- client components
- browser-accessible variables
- NEXT_PUBLIC_ environment variables
- source code

Use environment variables.

Example:

BREVO_API_KEY=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME=
CONTACT_RECEIVER_EMAIL=

Use actual variable names consistently.

Never commit secrets to Git.

Provide/update .env.example with placeholders only.

============================================================
31. CONTACT CONFIRMATION EMAIL
============================================================

After successful submission, send a warm confirmation email to the
visitor.

The email should feel human and welcoming.

Do NOT write a robotic message such as:

"Your ticket has been submitted."

The tone should communicate:

- gratitude
- warmth
- fellowship
- reassurance that the message was received

Do not make theological claims that aren't supported by the provided
content.

============================================================
32. RATE LIMITING
============================================================

Do not use Cloudflare Turnstile or other external CAPTCHA systems.

Implement basic but strict server-side rate limiting.

The contact endpoint must have protection against:

- excessive submissions
- obvious abuse
- malformed requests
- oversized payloads

Do not rely only on client-side restrictions.

The rate-limiting implementation should be appropriate for the chosen
deployment architecture.

============================================================
33. SERVER-SIDE SECURITY
============================================================

Validate all external input server-side.

Use Zod schemas.

Never trust:

- form data
- query parameters
- route parameters
- uploaded filenames
- uploaded file types
- admin input

Avoid leaking:

- API keys
- Supabase service keys
- internal errors
- stack traces
- implementation details

Return safe user-facing errors.

Log useful errors server-side where appropriate.

============================================================
34. ZOD
============================================================

Zod is mandatory.

Create reusable schemas for:

- contact form
- event creation
- event editing
- any other user/admin input

The schemas should be shared appropriately between client and server
where practical.

Do not duplicate validation rules unnecessarily.

============================================================
35. REACT HOOK FORM
============================================================

Use React Hook Form for forms.

At minimum:

- contact form
- event creation/editing form

Provide:

- validation messages
- loading states
- disabled submit state
- success state
- error state
- accessible labels

============================================================
36. SHADCN/UI
============================================================

shadcn/ui is mandatory.

Use shadcn components where they genuinely improve:

- forms
- buttons
- dialogs
- modals
- inputs
- textareas
- cards
- dropdowns
- alerts
- confirmation dialogs
- admin UI

Do not blindly use shadcn components for every visual element.

Custom components are encouraged when necessary.

Maintain a cohesive visual system.

============================================================
37. ACCESSIBILITY
============================================================

Accessibility is mandatory.

Use:

- semantic HTML
- proper heading hierarchy
- labels for inputs
- keyboard navigation
- visible focus states
- accessible dialogs
- accessible buttons
- accessible form errors
- meaningful alt text
- aria-label where needed
- appropriate aria-describedby relationships
- sufficient color contrast

Do not add ARIA attributes unnecessarily.

Prefer semantic HTML first.

Respect reduced-motion preferences.

============================================================
38. RESPONSIVENESS
============================================================

The website must be mobile-first.

Support:

- mobile
- tablet
- laptop
- desktop
- large desktop

Do not simply shrink desktop layouts.

Design intentionally for smaller screens.

Bento layouts must gracefully transform on mobile.

No horizontal overflow.

No broken navigation.

No tiny text.

No unusable modals.

No oversized desktop-only elements.

============================================================
39. NAVIGATION
============================================================

Desktop navigation should be clean and minimal.

Mobile navigation should be intentionally designed.

Include a prominent Donate CTA where appropriate without overwhelming
the navigation.

Use Lucide icons.

The current fellowship identity should be visible.

Do not overcrowd the navbar.

============================================================
40. TYPOGRAPHY
============================================================

DO NOT use Inter.

Do not default to the most common generic SaaS typography.

Choose a more distinctive typography pairing appropriate for:

- editorial design
- student fellowship
- modern church
- natural aesthetic

Use next/font.

Prefer a refined display font for major headings paired with a highly
readable body font.

Typography should provide character without sacrificing readability.

Avoid excessively chunky/chubby typography.

Do not use too many font families.

============================================================
41. ANIMATION
============================================================

Use Framer Motion.

Animations must be SUBTLE.

Preferred effects:

- fade in
- small translate
- small scale
- staggered card appearance
- image reveal
- subtle hover interaction

Use viewport/visible-in-view behavior where appropriate.

Avoid:

- excessive parallax
- aggressive bouncing
- spinning
- large movement
- unnecessary page transitions
- animation on every element

The user should feel the website is polished, not animated.

Respect prefers-reduced-motion.

============================================================
42. COMPONENT ARCHITECTURE
============================================================

Build reusable components.

Do not create one enormous page component.

Organize reusable UI logically.

Potential categories:

components/
  ui/
  layout/
  navigation/
  sections/
  events/
  units/
  gallery/
  forms/
  donation/
  admin/

Use clear naming.

Avoid unnecessary component fragmentation.

A component should have a meaningful responsibility.

============================================================
43. DATA ARCHITECTURE
============================================================

Centralize static information.

Potential structure:

src/
  data/
    content.ts
    images.ts
    social.ts
    donation.ts

Adapt the exact structure to the project.

Do not hardcode the same information in multiple components.

For example, the fellowship name should not be separately hardcoded
inside:

- navbar
- footer
- hero
- metadata
- mobile navigation

Instead, centralize reusable organizational configuration.

============================================================
44. SUPABASE STORAGE
============================================================

Use Supabase Storage for administrator-uploaded event images where
appropriate.

The public website should only access files that are intended to be
public.

Ensure storage policies are properly configured.

Do not expose privileged Supabase credentials.

Use secure server-side operations where required.

============================================================
45. DATABASE SCHEMA
============================================================

Keep the schema simple.

At minimum, an events table should support:

id
title
description
event_date
event_time
location
image_url / storage reference
external_url
category/type if useful
published
created_at
updated_at

Use appropriate types.

Do not store redundant information.

Use timestamps appropriately.

Do not store secrets in the database.

Authentication should be handled by Supabase Auth rather than inventing
a custom password system.

============================================================
46. ADMIN EVENT UX
============================================================

Creating/editing an event should be simple.

The administrator should not feel like they are using a complicated CMS.

The form should have:

- clear labels
- logical grouping
- image upload
- preview where useful
- save button
- cancel button
- loading state
- validation
- success feedback
- failure feedback

Deleting an event should require confirmation.

============================================================
47. ERROR HANDLING
============================================================

Every important asynchronous operation needs proper states.

Examples:

Loading:
"Saving event..."

Success:
"Event published successfully."

Failure:
"We couldn't save this event. Please try again."

Contact:
"Your message has been sent."

Contact failure:
"We couldn't send your message right now. Please try again."

Do not expose raw backend errors to visitors.

============================================================
48. SEO
============================================================

Implement proper SEO.

Include:

- title metadata
- descriptions
- canonical URLs
- Open Graph metadata
- social sharing metadata
- favicon
- sitemap
- robots.txt
- semantic HTML
- meaningful headings
- descriptive URLs

Every major public page should have appropriate metadata.

Do not use generic metadata such as:

"Next.js App"

============================================================
49. STRUCTURED DATA
============================================================

Use JSON-LD where appropriate.

Consider structured data for:

- organization
- events
- website
- relevant local/organizational information

Only include facts actually known.

Do not fabricate addresses, phone numbers, dates, or organizational
details.

============================================================
50. AGENTIC / LLM DISCOVERABILITY
============================================================

The website should be understandable by AI agents and search systems.

Use:

- semantic HTML
- descriptive page titles
- clear page descriptions
- structured data
- logical URLs
- machine-readable organization information
- clean navigation
- descriptive link text
- accessible content
- clear event information

Where appropriate, provide an llms.txt file containing a concise
description of the website and its important resources.

Do not stuff keywords.

Do not create artificial AI SEO content.

The site should simply be clear and well structured.

============================================================
51. PERFORMANCE
============================================================

Optimize for performance.

Use:

- next/image
- next/font
- lazy loading where appropriate
- server components by default
- client components only when necessary
- efficient data fetching
- minimal JavaScript where possible

Do not turn every component into a Client Component.

Use "use client" only when required.

Avoid unnecessary libraries.

============================================================
52. SERVER / CLIENT BOUNDARIES
============================================================

Prefer Server Components.

Client Components should be used for:

- interactive forms
- animations where necessary
- dialogs
- interactive admin functionality
- client-side state

Do not unnecessarily make entire pages client-side.

Keep API keys and sensitive operations server-side.

============================================================
53. API / BACKEND
============================================================

Use Next.js server functionality for application backend operations
unless a separate Express service is genuinely necessary.

Do not create Express merely for the sake of saying Express is used.

If Express is included, it must have a clear responsibility.

The preferred architecture is:

Frontend
   ↓
Next.js
   ↓
Server/API layer
   ├── Supabase
   └── Brevo

Avoid unnecessary duplication between Next.js API routes and Express.

============================================================
54. IMAGE OPTIMIZATION
============================================================

Use Next.js Image.

Provide meaningful alt text.

Do not use image paths directly everywhere.

Use the centralized images.ts system.

Ensure images have sensible sizing and responsive behavior.

Avoid huge unoptimized images being loaded on mobile.

============================================================
55. FOOTER
============================================================

Footer should contain:

- TACSFON identity
- short fellowship description
- navigation links
- social links
- Donate CTA
- compact contact form
- relevant contact information
- copyright
- current theme where appropriate

Do not make the footer overwhelmingly large.

Contact functionality belongs here rather than creating a giant contact
section on the homepage.

============================================================
56. CONTACT FORM UX
============================================================

The contact form should feel lightweight.

Do not ask unnecessary questions.

Suggested:

Name
Email
Phone (optional)
Subject
Message

Include:

- clear labels
- validation
- loading indicator
- success state
- error state
- accessible messages

Prevent accidental duplicate submissions.

============================================================
57. DONATION MODAL UX
============================================================

The donation modal should be beautiful and simple.

Suggested structure:

Support the Work

Short explanation.

Bank details / payment details.

Each important field should have:

[Copy]

After copying:

[Copied]

Include clear instructions for:

- donor name
- narration/reference
- what the donation is regarding

Do not force users to navigate away from the website just to see
donation details.

============================================================
58. CONTENT QUALITY
============================================================

Do not write filler.

Avoid phrases such as:

"Lorem ipsum"

"Welcome to our amazing church"

"Where faith meets excellence"

unless explicitly provided or genuinely appropriate.

Copy should feel like a real student fellowship.

Keep paragraphs readable.

Use concise headings.

Avoid huge walls of text.

============================================================
59. DESIGN ANTI-PATTERNS
============================================================

DO NOT:

- use generic stock images
- use Inter
- overuse rounded cards
- overuse glassmorphism
- overuse shadows
- make everything forest green
- make everything dark
- use bright yellow gold
- use excessive gold
- animate everything
- create a huge homepage
- create unnecessary pages
- create unnecessary backend services
- expose API keys
- create public admin signup
- hardcode dynamic event data
- hardcode donation information in multiple places
- scatter image paths throughout the project
- invent organizational facts
- use fake social media URLs
- use fake bank details
- create unnecessary complexity

============================================================
60. DEVELOPMENT PROCESS
============================================================

Follow this implementation order.

PHASE 1 — PROJECT INSPECTION

Before writing substantial code:

1. Inspect the existing project.
2. Inspect package.json.
3. Inspect existing Next.js configuration.
4. Inspect Tailwind configuration.
5. Inspect shadcn configuration.
6. Inspect provided image/assets directory.
7. Inspect content.txt.
8. Understand existing files before replacing anything.

Do not blindly overwrite an existing project.

============================================================

PHASE 2 — FOUNDATION

Establish:

- typography
- colors
- CSS variables
- spacing
- border radius
- layout containers
- responsive rules
- global styles
- reusable UI primitives

============================================================

PHASE 3 — DATA / CONFIGURATION

Create centralized configuration for:

- organization name
- theme
- contact information
- social links
- donation information
- static content
- image references

============================================================

PHASE 4 — PUBLIC UI

Build:

- navbar
- mobile navigation
- footer
- homepage
- about
- events
- units
- gallery
- resources/sermons if supported by provided content

============================================================

PHASE 5 — SUPABASE

Configure:

- authentication
- events table
- storage
- appropriate policies
- server-side access

============================================================

PHASE 6 — ADMIN

Build:

- admin login
- protected dashboard
- event listing
- event creation
- event editing
- event deletion
- publishing controls
- image upload

============================================================

PHASE 7 — CONTACT / BREVO

Implement:

- contact form
- Zod validation
- server-side validation
- rate limiting
- Brevo integration
- admin email
- visitor confirmation email

============================================================

PHASE 8 — DONATION

Implement:

- Donate CTA
- shadcn Dialog
- bank/payment details
- copy-to-clipboard
- donor instructions

============================================================

PHASE 9 — SEO / ACCESSIBILITY

Implement:

- metadata
- Open Graph
- sitemap
- robots
- JSON-LD
- accessibility
- semantic HTML
- llms.txt where appropriate

============================================================

PHASE 10 — POLISH

Review:

- spacing
- typography
- mobile layout
- tablet layout
- desktop layout
- Bento grids
- image cropping
- animation timing
- loading states
- empty states
- error states
- hover states
- focus states

============================================================
61. TESTING
============================================================

Before considering the project complete:

Run appropriate checks.

At minimum:

- TypeScript check
- ESLint
- production build
- form validation tests where appropriate
- admin access tests
- contact API tests
- event CRUD tests
- responsive inspection

Fix all build errors.

Do not declare success while known TypeScript/build errors remain.

============================================================
62. ADMIN SECURITY CHECKLIST
============================================================

Verify:

[ ] No public admin signup
[ ] Admin routes protected
[ ] Logout works
[ ] Unauthorized users cannot manage events
[ ] Supabase service credentials are not exposed
[ ] Brevo API key is server-side only
[ ] Event inputs validated
[ ] File uploads validated
[ ] Delete operation requires confirmation
[ ] Contact endpoint rate limited
[ ] Contact input validated server-side

============================================================
63. CONTACT SECURITY CHECKLIST
============================================================

Verify:

[ ] Zod validation
[ ] server-side validation
[ ] rate limiting
[ ] reasonable payload limits
[ ] safe error messages
[ ] no API key exposure
[ ] no arbitrary HTML injection
[ ] successful email delivery handling
[ ] failed email handling
[ ] visitor confirmation only after appropriate processing

============================================================
64. RESPONSIVE CHECKLIST
============================================================

Verify at:

- 320px+
- 375px+
- 390px+
- 414px+
- tablet
- 1024px
- 1280px
- 1440px
- 1920px

Check:

- navigation
- hero
- Bento grids
- events
- units
- gallery
- donation modal
- contact form
- footer
- admin dashboard

============================================================
65. FINAL QUALITY STANDARD
============================================================

The finished website should look like a professionally designed
university fellowship website.

It should NOT look like:

"AI generated a church website."

The design should have intentionality.

Every section should have a reason to exist.

Every color should have a purpose.

Every animation should have a purpose.

Every image should have a purpose.

Whitespace should be used deliberately.

Typography should carry significant visual identity.
============================================================
66. DEFINITION OF DONE
============================================================

IMPORTANT:

The project is already under active implementation.

DO NOT rebuild, restructure, or replace working parts of the existing
application merely because this specification contains requirements
that have already been implemented.

Before making changes:

1. Inspect the current implementation.
2. Identify what is already complete.
3. Preserve working architecture and components.
4. Reuse existing patterns, components, utilities, authentication,
   Supabase setup, styling and configuration where appropriate.
5. Only modify existing functionality when required to integrate the
   new programme payment system or fix an actual issue.
6. Do not duplicate functionality that already exists.

The remaining major feature to implement is:

PROGRAMME PAYMENT / REGISTRATION SYSTEM

After implementing the payment system, the entire application must
satisfy the following final checklist.

------------------------------------------------------------
PUBLIC WEBSITE
------------------------------------------------------------

[ ] Website runs successfully
[ ] Production build succeeds
[ ] TypeScript passes
[ ] ESLint passes
[ ] Mobile responsive
[ ] Tablet responsive
[ ] Desktop responsive
[ ] Existing public pages remain functional
[ ] Existing navigation remains functional
[ ] Existing Bento layouts remain functional
[ ] Existing local image system remains functional
[ ] No external stock images introduced
[ ] No fake organizational information introduced
[ ] No fake social URLs introduced
[ ] No fake payment information introduced
[ ] No obvious console errors
[ ] No horizontal overflow
[ ] Loading states implemented where required
[ ] Empty states implemented where required
[ ] Error states implemented where required

------------------------------------------------------------
DESIGN / UI
------------------------------------------------------------

[ ] Existing visual system is preserved
[ ] Forest green remains the primary color
[ ] White remains the primary supporting color
[ ] Muted/sophisticated gold remains an accent
[ ] No Inter font
[ ] shadcn/ui remains integrated
[ ] Framer Motion remains subtle
[ ] Lucide icons used where appropriate
[ ] Existing responsive behavior remains intact
[ ] Existing accessibility behavior remains intact

------------------------------------------------------------
CONTACT
------------------------------------------------------------

[ ] Contact form works
[ ] React Hook Form implemented
[ ] Zod validation implemented
[ ] Server-side validation implemented
[ ] Rate limiting implemented
[ ] Brevo integration works
[ ] Admin receives contact emails
[ ] Visitor confirmation email works
[ ] Brevo credentials remain server-side
[ ] Contact failures return safe user-facing messages

------------------------------------------------------------
DONATION
------------------------------------------------------------

[ ] Donate CTA works
[ ] Donation modal works
[ ] shadcn Dialog used
[ ] Donation information is centralized
[ ] Copy-to-clipboard works
[ ] Copy feedback works
[ ] No hardcoded duplicate donation information
[ ] Donation system remains separate from programme payments

------------------------------------------------------------
AUTHENTICATION / ADMIN
------------------------------------------------------------

[ ] Supabase authentication works
[ ] No public admin signup
[ ] Admin login works
[ ] Admin logout works
[ ] Admin routes are protected
[ ] Unauthorized users cannot access admin functionality
[ ] Supabase service-role credentials are not exposed
[ ] Existing event management continues to work
[ ] Admin can create events
[ ] Admin can edit events
[ ] Admin can delete events
[ ] Admin can publish/unpublish events
[ ] Event images continue to work
[ ] Unpublished events remain hidden publicly

------------------------------------------------------------
PROGRAMME PAYMENT SYSTEM
------------------------------------------------------------

[ ] Admin can create a programme payment request
[ ] Payment request has a unique UUID
[ ] UUID is URL-safe
[ ] UUID is used internally/publicly as the unique payment URL identifier
[ ] No unsafe '#' reference code is used in the URL
[ ] No separate human-readable payment reference code is required

Each payment request must support:

[ ] Programme name
[ ] Programme date
[ ] Description/instructions
[ ] Amount per person
[ ] Bank name
[ ] Account name
[ ] Account number
[ ] Optional programme image/flyer where appropriate
[ ] Created timestamp
[ ] Expiration timestamp
[ ] Status

The system must automatically set:

expires_at = created_at + 24 hours

The browser must NOT be responsible for determining whether a payment
request is expired.

The server/database must enforce the 24-hour expiration.

------------------------------------------------------------
PAYMENT REQUEST STATUS
------------------------------------------------------------

Supported statuses:

ACTIVE
CLOSED
EXPIRED

ACTIVE:
Visitors can open the payment page and submit registrations.

CLOSED:
The administrator has manually stopped the payment request.

EXPIRED:
24 hours have elapsed since creation.

Expired payment requests must no longer accept new submissions.

Do NOT automatically destroy historical records merely because a request
expires.

Historical registrations must remain available to authorized
administrators.

------------------------------------------------------------
ADMIN PAYMENT CREATION
------------------------------------------------------------

Admin route:

/admin/payments

Admin should be able to:

[ ] View payment requests
[ ] Create payment request
[ ] View payment request
[ ] Close payment request
[ ] Delete payment request
[ ] View registrations
[ ] View registered names
[ ] Export registered names
[ ] Open public payment page
[ ] Copy public payment URL
[ ] View generated QR code
[ ] Download generated QR code

IMPORTANT:

An existing payment request MUST NOT be editable.

If the administrator entered incorrect:

- amount
- bank details
- account details
- programme name
- programme date
- instructions

the administrator must close/delete the old request and create a new
payment request.

This guarantees that an old QR code or URL cannot continue displaying
outdated payment information.

Deleting a payment request must require confirmation.

------------------------------------------------------------
QR CODE
------------------------------------------------------------

A QR code must be generated automatically when a payment request is
created.

The QR code must encode the unique public payment URL.

Example concept:

https://site.com/pay/<uuid>

Do NOT encode sensitive information directly into the QR code.

The QR code should simply point to the public payment page.

Admin must be able to:

[ ] View QR
[ ] Download QR
[ ] Copy payment URL
[ ] Open payment page

------------------------------------------------------------
PUBLIC PAYMENT PAGE
------------------------------------------------------------

Create a public route similar to:

/pay/[paymentId]

The page must be mobile-first because users will commonly access it
after scanning a QR code.

The page should display:

[ ] Programme name
[ ] Programme date
[ ] Description/instructions
[ ] Amount per person
[ ] Bank name
[ ] Account name
[ ] Account number
[ ] Copy buttons for payment details
[ ] Expiration information/countdown
[ ] Number of people selector
[ ] Name fields
[ ] Automatically calculated total amount
[ ] Payment instructions
[ ] "Payment Completed" submission action
[ ] List of completed/registered names

Do not require account creation from public users.

------------------------------------------------------------
MULTIPLE-PERSON REGISTRATION
------------------------------------------------------------

A visitor must be able to register multiple people in one submission.

Example:

Number of people: 3

Name 1: John Doe
Name 2: Jane Doe
Name 3: Peter Doe

The system calculates:

total_amount =
amount_per_person × number_of_people

The client must NOT be trusted with the final amount.

The server must retrieve the authoritative amount-per-person from the
payment request and calculate the final amount itself.

Do not allow the client to submit an arbitrary total amount.

------------------------------------------------------------
NAME VALIDATION
------------------------------------------------------------

Names must be validated server-side.

Within one submission:

[ ] Names must not be empty
[ ] Names must be normalized/trimmed
[ ] Duplicate names within the same submission must be rejected
[ ] Reasonable name length limits must be enforced

The same person/name may appear in different submissions for the same
programme.

Do NOT globally reject a name merely because it already appears in
another submission.

------------------------------------------------------------
PAYMENT CONFIRMATION FLOW
------------------------------------------------------------

This is a MANUAL BANK TRANSFER system.

There is NO automated payment gateway.

Do NOT integrate:

- Paystack
- Flutterwave
- Stripe
- bank APIs
- payment verification APIs

unless explicitly requested later.

The flow is:

Visitor
  ↓
Opens unique payment URL
  ↓
Selects number of people
  ↓
Enters names
  ↓
System calculates total
  ↓
Visitor copies bank details
  ↓
Visitor makes bank transfer manually
  ↓
Visitor clicks "Payment Completed"
  ↓
Registration is submitted
  ↓
Names become visible on the public payment page
  ↓
Admin can view/export registrations

The interface must clearly communicate that clicking
"Payment Completed" means the visitor is confirming that they have
made the transfer.

Do not claim that the system automatically verified the bank transfer.

------------------------------------------------------------
PUBLIC REGISTERED NAMES
------------------------------------------------------------

After a successful submission, the registered names must become
visible on the public payment page.

Display names only.

Do NOT expose publicly:

- email addresses
- phone numbers
- internal UUIDs
- admin information
- database IDs
- internal metadata
- sensitive payment information beyond the intended payment details

The public list can communicate something simple such as:

Registered Participants

- John Doe
- Jane Doe
- Peter Doe

The 24-hour payment window provides the intended temporary context
for this public registration list.

------------------------------------------------------------
PAYMENT DATABASE
------------------------------------------------------------

Use Supabase PostgreSQL.

Keep the schema simple.

Recommended structure:

payment_requests

- id UUID primary key
- programme_name
- programme_date
- description
- amount_per_person
- bank_name
- account_name
- account_number
- payment_instructions
- image_url/storage_reference if needed
- status
- created_at
- expires_at
- closed_at

payment_submissions

- id UUID primary key
- payment_request_id
- total_people
- amount_per_person_snapshot
- total_amount
- created_at
- completed_at

payment_submission_people

- id UUID primary key
- submission_id
- name
- created_at

Use foreign keys and appropriate indexes.

The amount_per_person_snapshot is important because historical
registrations must retain the amount used when the submission was
created.

------------------------------------------------------------
PAYMENT SECURITY
------------------------------------------------------------

[ ] Payment requests are validated with Zod
[ ] Public submission input is validated server-side
[ ] Number of people has reasonable limits
[ ] Names have reasonable length limits
[ ] Total amount is calculated server-side
[ ] Client cannot override amount-per-person
[ ] Client cannot override payment request ownership/data
[ ] Expired requests reject submissions
[ ] Closed requests reject submissions
[ ] Deleted requests cannot receive submissions
[ ] Public users cannot edit existing registrations
[ ] Public users cannot modify payment requests
[ ] Public users cannot access admin data
[ ] Internal database IDs are not unnecessarily exposed
[ ] Sensitive credentials remain server-side
[ ] Rate limiting/abuse protection is applied to the public submission
    endpoint
[ ] Safe error messages are returned

------------------------------------------------------------
PAYMENT RLS / SUPABASE SECURITY
------------------------------------------------------------

Implement appropriate Row Level Security.

Public users may only access the minimum information necessary to:

- view an active payment request
- view its public completed names
- submit a registration to an active payment request

Public users must NOT be able to:

- create payment requests
- edit payment requests
- delete payment requests
- close payment requests
- edit existing submissions
- delete registrations
- access admin-only payment information

Authenticated authorized administrators may:

- create payment requests
- view payment requests
- close payment requests
- delete payment requests
- view registrations
- export registrations

Do not bypass security merely because the page is hidden from navigation.

------------------------------------------------------------
PAYMENT ADMIN UX
------------------------------------------------------------

The payment dashboard should remain lightweight.

Do NOT build a complicated payment analytics dashboard.

The admin should easily understand:

- programme
- programme date
- status
- creation time
- expiration time
- number of registered people
- registration list

IMPORTANT:

DO NOT DISPLAY AN "EXPECTED AMOUNT" OR EXPECTED REVENUE FIELD.

The admin only needs the registration information and the configured
amount-per-person where useful.

------------------------------------------------------------
EXPORT
------------------------------------------------------------

Admin must be able to export registered names.

Primary export format:

CSV

Each registered person should appear as a separate row.

Example columns:

Name
Programme
Programme Date
Amount Per Person
Total Submission Amount
Submission Time

Do NOT export unnecessary sensitive information.

The export must work for a selected payment request.

Excel-compatible CSV is sufficient.

Do not add a complex spreadsheet system.

------------------------------------------------------------
EXPIRATION UX
------------------------------------------------------------

The public payment page should communicate the 24-hour validity period.

A countdown may be displayed for user experience.

However:

The countdown is NOT the security mechanism.

The server/database expiration timestamp is authoritative.

When expired:

[ ] New registrations are blocked
[ ] Payment form is disabled
[ ] Payment details are no longer presented as an active payment option
[ ] Existing registered names remain available where appropriate
[ ] User receives a clear expired/unavailable message

Example:

"This payment request is no longer active."

Do not expose raw database errors.

------------------------------------------------------------
PAYMENT DATA LIFECYCLE
------------------------------------------------------------

Do not automatically delete payment records simply because they reach
24 hours.

The request becomes EXPIRED.

Historical records remain available to authorized administrators for
record keeping/export.

If automatic cleanup is introduced later, it must be explicitly
requested and must not silently destroy useful registration records.

------------------------------------------------------------
PAYMENT ROUTES
------------------------------------------------------------

Suggested routes:

/pay/[paymentId]

/admin/payments
/admin/payments/[id]

/admin/payments/new

Additional routes may be implemented if the current architecture
benefits from them.

Do not create unnecessary routes.

------------------------------------------------------------
PAYMENT UX STATES
------------------------------------------------------------

Implement proper states for:

[ ] Loading payment request
[ ] Payment request found
[ ] Payment request not found
[ ] Payment request expired
[ ] Payment request closed
[ ] Registration form loading
[ ] Registration submission
[ ] Registration success
[ ] Registration failure
[ ] Admin payment creation
[ ] Admin payment creation success
[ ] Admin payment creation failure
[ ] QR generation/loading
[ ] Export loading
[ ] Empty registration list

User-facing messages must be clear and human.

------------------------------------------------------------
INTEGRATION REQUIREMENT
------------------------------------------------------------

The programme payment system must integrate naturally into the
existing application.

DO NOT:

- redesign the entire website
- replace the existing admin dashboard
- replace the existing event system
- replace the existing authentication system
- introduce a second database
- introduce an unnecessary Express backend
- introduce a payment gateway
- duplicate existing UI components
- duplicate existing Supabase utilities
- duplicate existing validation utilities
- introduce unnecessary dependencies

Reuse the project's existing:

- Supabase client
- authentication utilities
- shadcn components
- form patterns
- Zod patterns
- Tailwind tokens
- layout components
- admin layout
- notification/toast system
- error handling patterns

where appropriate.

------------------------------------------------------------
ENVIRONMENT VARIABLES
------------------------------------------------------------

If QR generation or public URL construction requires an application
URL, use an environment variable rather than hardcoding a deployment
URL.

Update:

.env.example

with placeholder values only.

Never commit secrets.

------------------------------------------------------------
FINAL PAYMENT SYSTEM TEST
------------------------------------------------------------

Before declaring the payment system complete, manually verify this
entire flow:

1. Admin logs in.
2. Admin creates a payment request.
3. Programme name is saved.
4. Programme date is saved.
5. Amount per person is saved.
6. Bank details are saved.
7. Custom payment instructions are saved.
8. Unique UUID is generated.
9. Public URL is generated.
10. QR code is generated.
11. Admin can copy the URL.
12. Admin can view/download the QR.
13. Public URL opens successfully.
14. Public page displays the correct programme.
15. Public page displays the correct amount.
16. Public page displays the correct bank details.
17. Visitor selects multiple people.
18. Name fields are generated correctly.
19. Duplicate names in the same submission are rejected.
20. Total amount is calculated correctly.
21. Server recalculates the authoritative amount.
22. Visitor submits after making payment.
23. Submission succeeds.
24. Registered names become publicly visible.
25. Admin sees the registration.
26. Admin can export the names.
27. Closing the payment request blocks new submissions.
28. A request older than 24 hours becomes expired.
29. Expired requests block new submissions.
30. Existing historical registrations remain preserved.
31. Deleting a payment request requires confirmation.
32. Deleted payment requests cannot accept submissions.
33. Public users cannot access admin functionality.
34. No sensitive admin/database information is exposed.

============================================================
FINAL PROJECT DEFINITION OF DONE
============================================================

The project is COMPLETE only when:

[ ] Existing website implementation remains functional
[ ] Existing public pages remain functional
[ ] Existing event system remains functional
[ ] Existing authentication remains functional
[ ] Existing contact system remains functional
[ ] Existing donation system remains functional
[ ] Programme payment system is fully implemented
[ ] Programme payment system is secure
[ ] Programme payment system is mobile-friendly
[ ] Programme payment system is integrated with Supabase
[ ] Payment request UUID/URL generation works
[ ] QR generation works
[ ] 24-hour expiration works
[ ] ACTIVE/CLOSED/EXPIRED states work
[ ] Multiple-person registration works
[ ] Name uniqueness within submissions works
[ ] Server-side amount calculation works
[ ] Manual payment confirmation flow works
[ ] Public registered names work
[ ] Admin registration management works
[ ] CSV export works
[ ] Payment RLS/security works
[ ] Payment rate limiting/abuse protection works
[ ] All important payment loading/error/empty/success states work
[ ] No payment gateway has been introduced
[ ] No unnecessary backend infrastructure has been introduced
[ ] No duplicate architecture has been introduced
[ ] TypeScript passes
[ ] ESLint passes
[ ] Production build succeeds
[ ] No known blocking errors remain
[ ] No secrets are committed
[ ] .env.example is updated
[ ] Responsive behavior has been verified
[ ] Accessibility has been reviewed
[ ] Final UI polish has been completed

DO NOT DECLARE THE PROJECT COMPLETE UNTIL THE ABOVE CHECKLIST HAS
BEEN VERIFIED.
============================================================

# ACADEMIC HUB — ADDITIVE FEATURE
# ============================================================
# IMPORTANT:
# This section is an ADDITION to the existing TACSFON OAUSTECH
# website. Do not rebuild, redesign, remove, or break existing
# functionality.
#
# The Academic Hub is a separate product experience within the
# existing project and should have its own visual identity.
#
# DO NOT IMPLEMENT THE EXTERNAL ACADEMIC MATERIAL SORTER HERE.
#
# The external sorter is a completely separate project/process.
# This Next.js project only receives already-normalized files
# through the Admin dashboard.
# ============================================================


## 67. ACADEMIC HUB — PRODUCT OVERVIEW

Build a new academic materials platform called:

    Academic Hub

The connection to TACSFON OAUSTECH should be subtle.

Do NOT make the Academic Hub visually look like the main TACSFON
church website.

The Academic Hub should feel like a modern academic/product
platform with a clean editorial interface.

Small contextual branding may be used:

    TACSFON OAUSTECH

and/or:

    In partnership with TACSFON OAUSTECH

Do not make TACSFON the dominant visual identity of the Academic
Hub.

The Academic Hub provides students with organized academic
materials through a semester-based paid access system.

Users who purchase access to an active semester receive access
to all Academic Hub materials available for that semester.


## 68. ACADEMIC HUB VISUAL DIRECTION

The Academic Hub must have its own visual identity.

Primary visual direction:

    - Cream background
    - ChatGPT-inspired blue as the dominant accent
    - Very subtle/faint secondary blue
    - White cards/surfaces where appropriate
    - Clean typography
    - Modern editorial/academic feel
    - Spacious layouts
    - Strong hierarchy
    - Excellent mobile experience
    - Subtle motion only

Avoid:

    - Dark premium SaaS aesthetics
    - Excessive gradients
    - Generic AI-dashboard appearance
    - Excessive glassmorphism
    - Loud colors
    - Overly decorative church styling
    - Unnecessary animation

The Academic Hub should feel trustworthy, calm, organized and
easy to navigate.


## 69. ACADEMIC HUB MATERIAL TYPES

Initial supported material types:

    - Past Questions
    - Manuals / Lecture Notes
    - Workbooks
    - Materials

Do NOT create a Tutorials material category.

Material types should be data-driven rather than hardcoded throughout
the UI so additional types can be added later.


## 70. ACADEMIC MATERIAL HIERARCHY

The public Academic Hub should allow students to browse materials
using the following hierarchy:

    Material Type
        ↓
    Level
        ↓
    School
        ↓
    Department
        ↓
    Course
        ↓
    Academic Session / Semester

Example:

    Past Questions
        ↓
    300 Level
        ↓
    School of Science
        ↓
    Computer Science
        ↓
    CSC 308
        ↓
    2024/2025 — First Semester

Do not rely on physical folder structure to determine this hierarchy.

The database must store each metadata field independently.


## 71. ACADEMIC HUB NAVIGATION

The Academic Hub should provide an obvious top-level navigation/header
for material categories.

Example:

    Academic Hub

    [ Past Questions ]
    [ Manuals & Lecture Notes ]
    [ Workbooks ]
    [ Materials ]

The user then progressively filters/navigates through:

    Level
    School
    Department
    Course
    Session / Semester

The UI should remain clean and easy to understand on mobile.


## 72. SCHOOL / DEPARTMENT / COURSE MANAGEMENT

Do not assume the academic structure is permanent.

OAUSTECH may:

    - create departments
    - rename departments
    - remove departments
    - introduce new courses
    - rename courses
    - change course availability

Therefore:

    Schools
    Departments
    Courses

must be manageable from the Academic Hub Admin dashboard.

Do NOT hardcode the complete academic structure into components.

Admin should be able to:

    - create
    - edit
    - deactivate
    - reactivate

schools, departments and courses where appropriate.

Courses must be associated with their relevant department.

Departments must be associated with their relevant school.

Do not allow invalid relationships.


## 73. SEMESTER / ACADEMIC SESSION SYSTEM

Do NOT hardcode a "current semester" into the application.

Admin must be able to create and manage academic access periods.

An academic access period should contain at minimum:

    - academic session
    - semester name
    - start date
    - end date
    - access price
    - status
    - description/instructions where necessary
    - created_at
    - updated_at

Example:

    Academic Session:
        2026/2027

    Semester:
        First Semester

    Start:
        2026-10-12

    End:
        2027-01-11

    Price:
        Configured by Admin

    Status:
        ACTIVE

The exact dates and price must come from Admin configuration.

Never hardcode:

    2026/2027
    First Semester
    13 weeks
    any specific price

into application logic.

A semester may normally represent approximately 13 weeks, but
access must be controlled by the configured start/end dates.

If a student subscribes halfway through a semester, their access
must expire at the semester's configured end date.

Do NOT grant "13 weeks from the purchase date."

The semester itself defines the access period.


## 74. SEMESTER STATUS

Support semester statuses such as:

    DRAFT
    ACTIVE
    CLOSED
    EXPIRED

Only an appropriate active semester should be available for
new subscriptions.

Historical semesters must remain available in the database for
historical records and reporting.

Do not automatically delete historical data.


## 75. SEMESTER ACCESS / ENTITLEMENTS

A successful payment creates an entitlement for:

    user
        +
    semester

The entitlement must contain information such as:

    - id
    - user_id
    - semester_id
    - payment_reference
    - starts_at
    - expires_at
    - status
    - created_at

Access is granted only while:

    authenticated user
        AND
    valid entitlement
        AND
    current time < entitlement expiry

Do not grant access merely because a payment callback was visited.


## 76. ACADEMIC HUB PAYMENT

Use Paystack for Academic Hub payments.

The Academic Hub payment system is completely separate from:

    - TACSFON general donations
    - existing TACSFON programme payments

Do not merge the systems.

The Academic Hub must use Paystack's payment flow.

The price must come from the selected semester configuration.

Never trust an amount sent from the browser.

The server must retrieve the authoritative semester price from
the database before initializing a Paystack transaction.

Payment reference must be unique.

Include sufficient metadata to associate the transaction with:

    - Academic Hub user
    - semester
    - internal entitlement/payment record

Suggested metadata:

    userId
    semesterId
    internalPaymentId

Do not place sensitive information in payment metadata.


## 77. PAYSTACK ENVIRONMENT VARIABLES

Paystack credentials must be stored in environment variables.

Expected configuration:

    PAYSTACK_SECRET_KEY=
    NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=

The secret key must only be accessible server-side.

Never expose:

    PAYSTACK_SECRET_KEY

to client-side JavaScript.

Never hardcode Paystack keys in source code.

Never commit `.env.local` or secret credentials to Git.


## 78. PAYSTACK PAYMENT FLOW

Expected flow:

    User selects semester
            ↓
    User must be authenticated
            ↓
    Server retrieves semester
            ↓
    Server checks semester is ACTIVE
            ↓
    Server retrieves authoritative price
            ↓
    Server creates internal payment record
            ↓
    Server initializes Paystack transaction
            ↓
    User completes Paystack payment
            ↓
    Paystack callback/webhook
            ↓
    Server verifies transaction
            ↓
    Server confirms:
        - reference
        - amount
        - currency
        - transaction status
        - intended semester
        - intended user
    ↓
    Entitlement created/activated
            ↓
    Academic Hub unlocked


## 79. PAYMENT VERIFICATION

Never trust:

    - frontend success state
    - query parameters alone
    - browser redirect alone
    - client-supplied amount
    - client-supplied semester price

The server must verify the transaction with Paystack.

Implement a secure Paystack webhook endpoint.

Example:

    /api/paystack/webhook

Verify the webhook signature according to Paystack's current
documentation.

Also verify the transaction server-side before granting access.

Payment processing must be idempotent.

If the same successful Paystack event is received multiple times,
it must not create duplicate entitlements.


## 80. PAYMENT RECORDS

Create a dedicated Academic Hub payment table.

Suggested fields:

    id
    user_id
    semester_id
    paystack_reference
    amount
    currency
    status
    paid_at
    created_at
    updated_at

Use appropriate statuses such as:

    PENDING
    SUCCESS
    FAILED
    ABANDONED

Do not treat PENDING as paid access.


## 81. ACADEMIC HUB AUTHENTICATION

Academic Hub paid content requires an authenticated account.

A visitor may:

    - browse the public landing page
    - view the demo
    - inspect available categories
    - see the current semester offer

but must authenticate and have a valid semester entitlement
before accessing protected materials.


## 82. ACCESS SHARING PROTECTION

The system cannot completely prevent a user from manually giving
their credentials to another person, but it must make casual account
sharing difficult.

Implement:

    - authenticated accounts
    - email verification where supported by the existing auth system
    - protected material routes
    - protected Supabase Storage objects
    - entitlement checks on protected material requests
    - no publicly accessible original material URLs

Do not use public Supabase Storage URLs for paid materials.

Paid materials must be stored in a private bucket.

When a user requests a protected material:

    authenticate user
        ↓
    verify active semester entitlement
        ↓
    authorize material
        ↓
    generate a temporary/signed access URL
        ↓
    return the authorized resource

Do not expose permanent public file URLs.

Where practical, implement a reasonable active-session/device
limitation to discourage credential sharing.

Do not make the system unnecessarily hostile to legitimate users.


## 83. MATERIAL STORAGE

Do NOT store academic PDFs/images inside:

    public/
    app/
    src/
    the Git repository

Do not commit academic materials into the Next.js project.

Use Supabase Storage for the actual files.

Use a PRIVATE Supabase Storage bucket for protected materials.

Suggested bucket:

    academic-materials

Possible logical organization:

    academic-materials/
        <semester-id>/
            <material-id>/

The exact storage path is implementation-defined.

The database, not the physical folder structure, is the source
of truth for metadata.


## 84. MATERIAL DATABASE MODEL

Create a materials table containing structured metadata.

Suggested fields:

    id
    title
    material_type
    school_id
    department_id
    course_id
    level
    academic_session
    semester_id
    original_filename
    storage_path
    file_type
    mime_type
    file_size
    page_count
    is_published
    created_at
    updated_at

Additional useful fields may be added if genuinely necessary.

Do not duplicate entire material files inside PostgreSQL.


## 85. NORMALIZED FILE UPLOADS

The Academic Hub Admin dashboard receives files that have already
been normalized by an external process.

The external sorter is NOT part of this project.

The TACSFON project must assume that uploaded filenames follow the
approved naming convention.

Expected conceptual format:

    material_type__level__school__department__course__session.ext

Example:

    past_questions__500__science__computer_science__CSC_308__2024-2025.pdf

Another example:

    manual__300__science__computer_science__CSC_301__2024-2025.pdf

The delimiter should be consistent and parser-safe.

Do not use ambiguous delimiters that could conflict with metadata.


## 86. FILENAME PARSER

Create a small TypeScript parser/validator for normalized filenames.

The parser should:

    - remove the extension
    - split the normalized filename
    - identify each metadata segment
    - normalize where appropriate
    - validate required fields
    - resolve IDs against the database
    - reject invalid files
    - produce structured metadata

Do not use arbitrary `.includes()` checks as the primary parser.

Use deterministic parsing based on the agreed filename structure.

Use database lookups for:

    school
    department
    course
    material type
    semester/session

If a filename contains an unknown school, department, course or
material type:

    do NOT invent a value.

Instead:

    flag the upload for Admin correction.


## 87. ADMIN MATERIAL UPLOAD

Add an Academic Materials management area to the Admin dashboard.

Provide:

    + Upload Materials

Admin can select multiple already-normalized files.

After selection:

    1. Parse filenames
    2. Validate metadata
    3. Resolve academic entities
    4. Display a review table
    5. Allow Admin to correct metadata
    6. Upload approved files to private Supabase Storage
    7. Create database records
    8. Publish the materials

Do not immediately publish files simply because they were selected.


## 88. MATERIAL UPLOAD REVIEW

Before publishing, show a clear review interface.

Example:

    File:
    past_questions__500__science__computer_science__CSC_308__2024-2025.pdf

    Type:
    Past Questions

    Level:
    500

    School:
    School of Science

    Department:
    Computer Science

    Course:
    CSC 308

    Session:
    2024/2025

    Semester:
    First Semester

Actions:

    [Edit]
    [Approve]
    [Reject]

Allow the Admin to correct metadata before publishing.


## 89. DUPLICATE MATERIAL PROTECTION

Prevent accidental duplicate uploads.

Use a combination of:

    - normalized metadata
    - filename
    - file checksum/hash where appropriate

If a duplicate or likely duplicate is detected:

    warn the Admin

Do not silently overwrite existing academic material.

Require an explicit Admin decision.


## 90. MATERIAL PUBLISHING

A material should only become visible to students when:

    - metadata is valid
    - file upload succeeded
    - database record exists
    - material is marked published

Do not expose partially uploaded materials.


## 91. MATERIAL PREVIEW

Students should be able to preview:

    - PDF files
    - image files

without downloading the original file.

PDF preview should use an appropriate browser-compatible viewer
or controlled preview mechanism.

Images should be displayed responsively.

Ensure previews remain protected behind authentication and
semester entitlement checks.

Do not expose the original private Storage URL permanently.


## 92. DOWNLOADS — CURRENTLY DISABLED

Downloads should be architecturally considered but NOT enabled
in the initial Academic Hub release.

Future download capability may be implemented as:

    // FUTURE FEATURE — DOWNLOADS
    // Downloads are currently disabled.
    //
    // When enabled:
    // - authenticate user
    // - verify active semester entitlement
    // - generate a watermarked derivative
    // - never expose the original master file
    // - log the download where appropriate

Do not show an active Download button in the initial release.

If a download feature is implemented later, downloaded files must
contain the watermark:

    TACSFON × FC STUDIO


## 93. WATERMARK ARCHITECTURE

Original master files stored in Supabase must remain clean.

Do NOT permanently watermark the original uploaded file.

Future downloadable derivatives should be generated from the
clean master.

Watermark text:

    TACSFON × FC STUDIO

The watermark should be subtle enough not to interfere with
academic readability.

PDFs may use a subtle repeated diagonal watermark.

Images may use a subtle overlay watermark.

Do not alter the master file.


## 94. DEMO / PREVIEW BEFORE PAYMENT

The Academic Hub must provide a demonstration area before payment.

Unauthenticated visitors should be able to see examples of what
they receive after subscribing.

Include:

    - demo video
    - demo PDF
    - demo image

These must be clearly marked as:

    DEMO
    SAMPLE
    EXAMPLE

or equivalent.

Demo materials must NOT grant access to the protected academic
library.

The demo should communicate the quality and organization of the
actual service without exposing the full paid library.


## 95. ACADEMIC HUB LANDING PAGE

The landing page should clearly explain:

    - what Academic Hub is
    - what material categories are available
    - how semester access works
    - what the current semester access period is
    - the configured price
    - what students receive
    - demo materials
    - payment flow
    - support/contact information

Example messaging:

    "Get organized access to academic resources for your semester."

Explain that one active semester subscription provides access to
all published Academic Hub materials available for that semester.

Do not claim materials exist when they do not.


## 96. SEMESTER ACCESS UX

Display the configured semester information dynamically.

Example:

    2026/2027 — First Semester

    Access period:
    12 October 2026 — 11 January 2027

    Full semester access

    ₦X,XXX

    [ Get Access ]

The dates and price must come from the database.

Do not hardcode them in the UI.


## 97. EXPIRED ACCESS

When a semester expires:

    - the user's entitlement becomes expired
    - protected material access is denied
    - the user may still access their account
    - payment history remains available
    - previous semester records remain preserved

If a new semester is available, show an appropriate option to
purchase the new semester.

Do not delete old entitlements.


## 98. ADMIN ACADEMIC HUB DASHBOARD

Add Academic Hub management to the existing Admin system.

Admin should be able to manage:

    - academic sessions
    - semesters
    - semester prices
    - semester dates
    - schools
    - departments
    - courses
    - material types
    - materials
    - demo materials
    - published/unpublished state
    - Academic Hub payment records
    - user semester entitlements

Do not expose these controls publicly.


## 99. ADMIN SEMESTER MANAGEMENT

Admin must be able to:

    - create semester
    - edit semester
    - activate semester
    - close semester
    - view historical semesters

Fields:

    academic session
    semester
    start date
    end date
    price
    status
    description

Validate:

    end date > start date
    price >= 0

Avoid allowing contradictory overlapping ACTIVE semesters unless
the implementation explicitly supports them.


## 100. ADMIN ACADEMIC STRUCTURE

Admin management screens should support:

    Schools
        ↓
    Departments
        ↓
    Courses

Use relational references rather than storing free-form strings
where practical.

Allow deactivation rather than destructive deletion when an entity
is already referenced by published materials.

Historical materials must remain valid if an academic entity is
later renamed/deactivated.


## 101. SECURITY / SUPABASE RLS

Implement appropriate Supabase Row Level Security.

Students/public users must NOT be able to:

    - create semesters
    - change prices
    - create schools/departments/courses
    - publish materials
    - replace materials
    - modify material metadata
    - access another user's entitlement
    - alter payment records
    - bypass semester access

Admin operations must be protected by the existing authenticated
Admin system.

Paid materials must remain protected.

Do not rely solely on client-side route protection.

Server-side authorization is required.


## 102. PAYMENT SECURITY

Never trust the client for:

    - payment amount
    - semester ID
    - entitlement expiry
    - payment status

The server must determine all authoritative values.

Verify Paystack transactions server-side.

Validate:

    - transaction reference
    - transaction status
    - amount
    - currency
    - associated user
    - associated semester

Prevent duplicate entitlement creation.


## 103. SUPPORT INFORMATION

Academic Hub support:

    Email:
    festusphillip19@gmail.com

    Phone:
    09153741926

Use this information in appropriate support/payment help areas.


## 104. ACCESS / SUBSCRIPTION COPY

Use clear, honest language.

Students should understand:

    - payment grants access to the selected semester
    - access lasts only until that semester's configured end date
    - one semester subscription provides access to all published
      Academic Hub materials available for that semester
    - access is tied to their account
    - demo content is available before payment

Do not promise specific materials unless they are actually present.


## 105. ROUTING

The Academic Hub should have its own route structure.

The exact route names may be selected to fit the existing project,
but the Academic Hub must not clutter the main TACSFON church
navigation.

Possible conceptual structure:

    /academic
    /academic/...
    
or another clean dedicated route.

Do not expose every Academic Hub admin route publicly.

The Academic Hub may be reached from a subtle link/button where
appropriate, but it should remain visually distinct from the main
TACSFON website.


## 106. COMPONENT / ARCHITECTURE GUIDELINES

Continue following the existing project architecture:

    - Server Components by default
    - Client Components only where interactivity is required
    - TypeScript
    - Tailwind CSS
    - shadcn/ui
    - Zod
    - React Hook Form where forms are required
    - Lucide React
    - Framer Motion for restrained animation
    - Supabase
    - Next.js App Router

Do not introduce another UI framework.

Do not create unnecessary dependencies.


## 107. API / SERVER LOGIC

Use Next.js server functionality where appropriate.

Create clear server-side boundaries for:

    - Paystack initialization
    - Paystack verification
    - Paystack webhook
    - entitlement creation
    - protected material authorization
    - material publishing
    - admin operations

Do not expose Supabase service-role credentials to the client.

Do not expose Paystack secret credentials to the client.


## 108. ERROR HANDLING

Provide useful errors for:

    - invalid filename
    - unknown school
    - unknown department
    - unknown course
    - unknown material type
    - invalid semester
    - duplicate material
    - upload failure
    - payment initialization failure
    - payment verification failure
    - expired entitlement
    - unauthorized material access

Errors should be human-readable.

Do not expose sensitive server information.


## 109. RESPONSIVE / ACCESSIBLE UI

Academic Hub must be mobile-first.

Ensure:

    - readable typography
    - large touch targets
    - keyboard navigation
    - visible focus states
    - semantic headings
    - accessible dialogs
    - accessible forms
    - appropriate loading states
    - appropriate empty states
    - appropriate error states

The material browser must work particularly well on mobile because
students may primarily access the service from phones.


## 110. SEO / DISCOVERABILITY

The public Academic Hub landing page should have appropriate:

    - title
    - description
    - Open Graph metadata
    - semantic headings

Protected academic materials should not be publicly indexed.

Do not expose private material URLs to search engines.


## 111. EMPTY STATES

Do not display fake academic content.

If a category contains no materials:

    "No materials available yet."

If a course has no material:

    "No materials have been published for this course yet."

Provide useful navigation back to the previous selection.


## 112. LOADING STATES

Use polished loading states for:

    - payment initialization
    - material upload
    - material publishing
    - protected preview loading
    - academic structure loading

Avoid excessive skeleton screens where a simple loading state is
more appropriate.


## 113. DEMO MATERIAL MANAGEMENT

Demo resources should be manageable by Admin.

Admin should be able to:

    - upload demo video
    - upload demo PDF
    - upload demo image
    - replace demo resources
    - remove demo resources
    - control whether a demo resource is visible

Demo resources must remain separate from paid materials.


## 114. AUDIT / TRACEABILITY

Where practical, record:

    - who uploaded a material
    - when it was uploaded
    - who published it
    - when it was published
    - who modified metadata

Do not sacrifice simplicity for unnecessary enterprise auditing.


## 115. DO NOT CONFUSE SYSTEMS

The following systems must remain separate:

    1. TACSFON general donation system
    2. TACSFON programme payment system
    3. Academic Hub semester subscription system

Each must have its own:

    - database records
    - payment logic
    - authorization logic
    - UI flow

Do not reuse programme-payment assumptions for Academic Hub.


## 116. IMPLEMENTATION ORDER

Implement the Academic Hub in this order:

    Phase 1:
        Database schema
        RLS
        Academic sessions
        Semesters
        Schools
        Departments
        Courses
        Material types

    Phase 2:
        Academic Hub visual shell
        Landing page
        Demo section
        Material browsing UI

    Phase 3:
        Admin academic management
        Material upload
        Filename parser
        Metadata review
        Supabase Storage

    Phase 4:
        Authentication
        Semester entitlements
        Protected materials

    Phase 5:
        Paystack initialization
        Paystack verification
        Webhook
        Payment records
        Entitlement activation

    Phase 6:
        PDF/image preview
        Access expiry
        Error/loading/empty states

    Phase 7:
        Security review
        RLS review
        Mobile review
        Accessibility review
        SEO review

Do not skip database/security design and attempt to build the
payment UI first.


## 117. FUTURE DOWNLOAD ARCHITECTURE

Do not implement active downloads in the first release.

Keep the system structured so that downloads can later be added
without changing the master storage architecture.

Future flow:

    User requests download
        ↓
    Authenticate
        ↓
    Verify entitlement
        ↓
    Retrieve clean master
        ↓
    Generate watermarked derivative
        ↓
    Deliver temporary download
        ↓
    Log download if required

Never provide direct access to the clean master.


## 118. FINAL DEFINITION OF DONE

The Academic Hub feature is considered complete only when:

[ ] Academic Hub has its own cream + blue visual identity.

[ ] TACSFON OAUSTECH branding is subtle and contextual.

[ ] Main TACSFON website functionality remains intact.

[ ] Academic Hub is separate from the main church-site visual
    experience.

[ ] Admin can create academic sessions.

[ ] Admin can create/manage semesters.

[ ] Semester price is configurable by Admin.

[ ] Semester start/end dates are configurable by Admin.

[ ] No current semester is hardcoded.

[ ] Schools are manageable by Admin.

[ ] Departments are manageable by Admin.

[ ] Courses are manageable by Admin.

[ ] Material types are manageable/configurable.

[ ] Normalized filenames can be parsed automatically.

[ ] Invalid filenames are rejected or sent for correction.

[ ] Admin can review uploaded material metadata before publishing.

[ ] Materials are stored in private Supabase Storage.

[ ] Material metadata is stored in Supabase Database.

[ ] Published materials are associated with the correct semester.

[ ] Students can browse:
        type → level → school → department → course → session/semester

[ ] Demo video is available before payment.

[ ] Demo PDF is available before payment.

[ ] Demo image is available before payment.

[ ] Demo resources are clearly marked as samples.

[ ] Students can create/authenticate an Academic Hub account.

[ ] Paystack test integration works.

[ ] Paystack secret key is server-only.

[ ] Paystack transactions are verified server-side.

[ ] Webhook signature is verified.

[ ] Payment processing is idempotent.

[ ] Successful payment creates the correct semester entitlement.

[ ] Student access expires at the configured semester end date.

[ ] Student cannot access paid materials without valid entitlement.

[ ] Paid material files are not publicly accessible.

[ ] Protected material access uses authorization checks.

[ ] Direct public Storage URLs are not exposed.

[ ] Expired subscriptions lose protected material access.

[ ] Historical payments and entitlements remain preserved.

[ ] Downloads remain disabled in the initial release.

[ ] Future download architecture supports watermarking.

[ ] Watermark text is:
        TACSFON × FC STUDIO

[ ] Original master files remain unwatermarked.

[ ] No academic PDFs/images are committed to the Git repository.

[ ] No fake academic data is introduced.

[ ] No Resources/Sermons section is created.

[ ] Existing TACSFON functionality is not broken or unnecessarily
    rewritten.

[ ] Mobile experience is polished.

[ ] Accessibility basics are implemented.

[ ] Public landing page has appropriate SEO metadata.

[ ] Protected academic materials are not publicly indexed.

[ ] Appropriate loading, empty and error states exist.

[ ] Academic Hub support information is displayed:

        festusphillip19@gmail.com
        09153741926